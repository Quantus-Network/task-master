# TaskMaster Configuration

[server]
host = "127.0.0.1"
port = 3000

[blockchain]
website_url = "https://www.quantus.com"

# Quantus node WebSocket URL
node_url = "wss://a1-dirac.quantus.cat"

# Wallet configuration for transactions
wallet_name = "test_seed_wallet3"
wallet_password = "test123"

# Reversal period for transactions (in hours)
reversal_period_hours = 12

[candidates]
# GraphQL endpoint to fetch candidate addresses
graphql_url = "https://subsquid.quantus.com/graphql"
# How often to refresh candidates (in minutes)
refresh_interval_minutes = 30

[task_generation]
# How often to generate new tasks (in minutes)
generation_interval_minutes = 60
# Number of taskees to select each round
taskees_per_round = 5

[reverser]
# How many minutes before end_time to reverse uncompleted tasks
early_reversal_minutes = 2
# How often to check for tasks to reverse (in seconds)
check_interval_seconds = 30

[data]
# Database configuration
database_url = "postgres://postgres:postgres@127.0.0.1:55432/task_master"

[logging]
# Log level: error, warn, info, debug, trace
level = "info"

[jwt]
secret = "this-should-be-overriden"
exp_in_hours = 24

[x_oauth]
client_id = "WlVrcm4xSEpXQ2l3TURFM3lLZnE6MTpjaQ"
client_secret = "9xx6rNxYKG_dpuDE7VgGKnIqhV0AekJc571xJz5agVYkwCm_Ym"
callback_url = "http://localhost:3000/api/auth/x/callback"